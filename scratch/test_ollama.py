import base64
import httpx
import asyncio
import json

async def test_vision():
    image_path = "/Users/ritchie/Desktop/live website /posendwebsite/menu-scanner/backend/temp_uploads/temp_menu.jpg" # dummy or we need a real image
    # Let's just test with a real image if we have one, otherwise we'll just see if it accepts the format
    
    # We can just write the logic that we'll put in the service
    print("Test script ready")

if __name__ == "__main__":
    asyncio.run(test_vision())
